//Arrow  function
const unt= "sq ft";

const calculateArea = (length, width) => `${length * width} ${unit}`;;
const area1 = calculateArea(10, 5);
const area2 = calculateArea(10, 10);

console.log(area1);
console.log(area2);